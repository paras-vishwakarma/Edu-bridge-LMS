// Vercel serverless function entry point.
// Imports and re-exports the Express app from server.js.
// Vercel's Node.js runtime calls this as a serverless handler.
export { default } from '../server.js';
